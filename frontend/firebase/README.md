Drop your Firebase app config files here before building push-enabled binaries.

Required for Android push:
- `google-services.json` (from Firebase Console for package `com.finchwire.app`)

Optional (only if you wire native iOS Firebase services later):
- `GoogleService-Info.plist`

These files are intentionally gitignored.
